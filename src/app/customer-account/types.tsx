export interface CustomerData {
    fullName:string;
    imageUrl: string;
    email: string;
    password: string;
    confirmPassword: string;
    buildingNumber: string;
    floorNumber: string;
    roomNumber: string;
    phoneNumber: string; // Define phoneNumber as string consistently
  }